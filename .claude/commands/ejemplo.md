---
argument-hint: <texto>
---

Recibiste el argumento: $ARGUMENTS

Este es un slash command de ejemplo. Al escribir `/ejemplo hola` en Claude Code,
este prompt se ejecuta con "hola" sustituido en $ARGUMENTS.

Reemplaza este contenido por las instrucciones reales del comando.
